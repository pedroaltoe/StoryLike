import Foundation

enum Localized {
    enum Stories {
        static let emptyMessage = String(localized: "Sorry, something went wrong, there are no images to show.")
        static let missingDataError = String(localized: "Missing data error")
        static let refresh = String(localized: "Refresh")
    }
}
