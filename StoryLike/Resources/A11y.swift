import Foundation

enum A11y {
    enum Stories {
        static let goBackButton = "Go back button"
        static let loading = "Loading"
        static let refreshButton = "Refresh screen button"
    }
}
